import { Typography } from "@mui/material";
import React from "react";

const NotFound = () => {
    return (
        <Typography color="primary" mt={2} textAlign={"center"} fontSize={"32px"}>
            صفحه مورد نطر یافت نشد !
        </Typography>
    );
};

export default NotFound;
